import { CreatorStageNode, CustomStageNode } from "@/app/builder/[idProcessType]/components"

const nodeTypes = {
  stageNode: CustomStageNode,
  nodeCreator: CreatorStageNode,
}
 
export default nodeTypes
