import { Box } from '@mui/material'

const ContentManagement = ({ children }) => {
  return (
    <Box
      px={5}
      py={2}
    >
      {children}
    </Box>
  )
}

export default ContentManagement
